      <main className="grid grid-cols-1 lg:grid-cols-12 gap-10 items-start">
        {/* Left Column: Sizing & Stone Controls */}
        <section className="lg:col-span-7 bg-white/75 backdrop-blur-sm border border-gold-100 rounded-3xl shadow-sm overflow-hidden flex flex-col">
          
          {/* Luxury Stepped Navigation Switcher */}
          <div className="flex border-b border-gold-100 bg-white/50">
            <button 
              onClick={() => setActiveTab('stones')}
              className={`flex-1 py-4 text-xs font-mono tracking-widest uppercase font-semibold transition-all ${activeTab === 'stones' ? 'bg-[var(--theme-soft)] text-[var(--theme-primary)] border-b-2 border-[var(--theme-primary)]' : 'text-obsidian-500 hover:bg-gold-50/50'}`}
            >
              1. Stones
            </button>
            <button 
              onClick={() => setActiveTab('accents')}
              className={`flex-1 py-4 text-xs font-mono tracking-widest uppercase font-semibold transition-all ${activeTab === 'accents' ? 'bg-[var(--theme-soft)] text-[var(--theme-primary)] border-b-2 border-[var(--theme-primary)]' : 'text-obsidian-500 hover:bg-gold-50/50'}`}
            >
              2. Accents
            </button>
            <button 
              onClick={() => setActiveTab('fit')}
              className={`flex-1 py-4 text-xs font-mono tracking-widest uppercase font-semibold transition-all ${activeTab === 'fit' ? 'bg-[var(--theme-soft)] text-[var(--theme-primary)] border-b-2 border-[var(--theme-primary)]' : 'text-obsidian-500 hover:bg-gold-50/50'}`}
            >
              3. Fit
            </button>
          </div>

          <div className="p-6 md:p-8 space-y-8 animate-in fade-in duration-500">
            {activeTab === 'stones' && (
              <>
                {/* Preset Designs Row */}
          <div>
            <h3 className="font-mono text-xs text-gold-600 uppercase tracking-widest font-semibold mb-4 flex items-center gap-2">
              <Sparkles className="w-3.5 h-3.5" /> Curated Studio Presets
            </h3>
            <div className="grid grid-cols-2 sm:grid-cols-4 gap-3">
              {DESIGN_PRESETS.map((p) => {
                const firstStone = GEMSTONE_DB[p.pattern[0]] || { hex: '#b8bcc3' };
                const secStone = GEMSTONE_DB[p.pattern[1]] || firstStone;
                return (
                  <button
                    key={p.name}
                    onClick={() => applyPreset(p)}
                    className="group text-left p-3.5 border border-gold-200/50 rounded-2xl bg-white hover:bg-gold-50/50 hover:border-gold-400 transition-all duration-300"
                  >
                    <div className="flex gap-1.5 mb-2.5">
                      <span className="w-3.5 h-3.5 rounded-full shadow-inner" style={{ backgroundColor: firstStone.hex }} />
                      <span className="w-3.5 h-3.5 rounded-full shadow-inner" style={{ backgroundColor: secStone.hex }} />
                    </div>
                    <h4 className="font-serif text-sm font-semibold text-obsidian-900 leading-tight group-hover:text-gold-600 transition-colors">
                      {p.name}
                    </h4>
                    <span className="font-mono text-[10px] text-obsidian-400 block mt-0.5">{p.sub}</span>
                  </button>
                );
              })}
            </div>
          </div>

              </>
            )}

            {activeTab === 'fit' && (
              <>
                {/* Sizing & Comfort Console */}
          <div className="space-y-6">
            <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
              <h3 className="font-mono text-xs text-gold-600 uppercase tracking-widest font-semibold flex items-center gap-2">
                <Sliders className="w-3.5 h-3.5" /> 1. Sizing Calibration
              </h3>
              
              {/* Unit Toggles */}
              <div className="inline-flex bg-obsidian-50 p-1 rounded-xl border border-obsidian-100">
                {(['cm', 'mm', 'in'] as const).map((u) => (
                  <button
                    key={u}
                    onClick={() => {
                      setUnit(u);
                      showToast(`Switched unit to ${u.toUpperCase()}`);
                    }}
                    className={`px-3 py-1 text-xs font-mono rounded-lg transition-all ${
                      unit === u ? 'bg-white shadow-sm text-gold-600 font-semibold' : 'text-obsidian-400 hover:text-obsidian-700'
                    }`}
                  >
                    {u}
                  </button>
                ))}
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-12 gap-6 items-center">
              {/* Numerical Wrist Value Input */}
              <div className="md:col-span-5 bg-gold-50/50 border border-gold-200/50 p-4 rounded-2xl flex items-baseline justify-between">
                <div>
                  <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider">Wrist Circumference</span>
                  <input
                    type="number"
                    step="0.1"
                    value={formattedWristVal()}
                    onChange={(e) => handleWristInputChange(e.target.value)}
                    className="bg-transparent text-3xl font-serif font-bold text-obsidian-900 w-28 focus:outline-none"
                  />
                </div>
                <span className="font-mono text-sm text-gold-500 uppercase font-semibold">{unit}</span>
              </div>

              {/* Range Slider for Micro-calibrating */}
              <div className="md:col-span-7 space-y-2">
                <input
                  type="range"
                  min="130"
                  max="220"
                  step="1"
                  value={wristMm}
                  onChange={(e) => setWristMm(parseInt(e.target.value))}
                  className="w-full h-1.5 bg-gold-100 rounded-lg appearance-none cursor-pointer accent-gold-500"
                />
                <div className="flex justify-between font-mono text-[10px] text-obsidian-400 uppercase">
                  <span>Petite (13.0cm)</span>
                  <span className="text-gold-600 font-semibold">Standard (16.5cm)</span>
                  <span>Generous (22.0cm)</span>
                </div>
              </div>
            </div>

            {/* Comfort Ease selection */}
            <div>
              <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider mb-2.5">Comfort Allowance (Gap Ease)</span>
              <div className="grid grid-cols-3 gap-3">
                {[
                  { label: 'Snug Fit', val: 3, desc: '+3mm ease (skin tight)' },
                  { label: 'Standard Fit', val: 6, desc: '+6mm ease (drapes nicely)' },
                  { label: 'Relaxed Fit', val: 12, desc: '+12mm ease (loose slip)' }
                ].map((opt) => (
                  <button
                    key={opt.val}
                    onClick={() => setEase(opt.val)}
                    className={`p-3 text-left border rounded-2xl transition-all duration-300 ${
                      ease === opt.val 
                        ? 'bg-obsidian-900 border-obsidian-900 text-gold-100 shadow-md' 
                        : 'border-gold-200/50 bg-white hover:border-gold-400 text-obsidian-800'
                    }`}
                  >
                    <span className="block text-xs font-semibold">{opt.label}</span>
                    <span className="block text-[10px] font-mono mt-0.5 opacity-60">{opt.desc}</span>
                  </button>
                ))}
              </div>
            </div>
          </div>

          <hr className="border-gold-200/30" />

          {/* Primary Gemstone Selector */}
          <div className="space-y-5">
            <h3 className="font-mono text-xs text-gold-600 uppercase tracking-widest font-semibold flex items-center gap-2">
              <Sparkle className="w-3.5 h-3.5" /> 2. Gemstone Selection
            </h3>

            {/* Micro Swatches */}
            <div>
              <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider mb-3">Primary Mineral Strand</span>
              <div className="flex flex-wrap gap-2.5">
                {gemstoneNames.map((name) => {
                  const dbInfo = GEMSTONE_DB[name];
                  const active = mainStone === name;
                  return (
                    <button
                      key={name}
                      onClick={() => {
                        setMainStone(name);
                        // Make sure size and quality exist
                        const sizes = [...new Set(catalog.filter(i => i.type === 'stone' && i.name === name).map(i => i.size))];
                        if (!sizes.includes(mainSize)) {
                          setMainSize(sizes[0] || 8);
                        }
                        showToast(`Selected ${name}`);
                      }}
                      className={`relative flex items-center gap-2.5 px-3.5 py-2 border rounded-full transition-all duration-300 ${
                        active 
                          ? 'border-gold-500 bg-gold-100/40 text-gold-800 font-medium' 
                          : 'border-gold-200/40 bg-white hover:border-gold-300 text-obsidian-700'
                      }`}
                    >
                      <span className="w-3.5 h-3.5 rounded-full shadow-inner relative flex" style={{ backgroundColor: dbInfo?.hex }}>
                        <span className="absolute top-0.5 left-0.5 w-1 h-1 rounded-full bg-white/60" />
                      </span>
                      <span className="text-xs font-serif">{name}</span>
                    </button>
                  );
                })}
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
              {/* Size Selector */}
              <div>
                <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider mb-2.5">Bead Caliber (Diameter)</span>
                <div className="flex flex-wrap gap-2">
                  {sizesForActiveStone.map((sz) => {
                    const isRare = sz <= 3;
                    return (
                      <button
                        key={sz}
                        onClick={() => setMainSize(sz)}
                        className={`flex-1 min-w-[50px] py-2 text-center rounded-xl border text-xs font-mono transition-all ${
                          mainSize === sz
                            ? 'bg-obsidian-900 text-gold-100 border-obsidian-900 shadow-sm'
                            : isRare 
                              ? 'border-amber-400 text-amber-700 bg-amber-50/30 hover:border-amber-500'
                              : 'border-gold-200/50 bg-white hover:border-gold-400 text-obsidian-700'
                        }`}
                      >
                        <span className="block text-sm font-semibold">{sz}</span>
                        <span className="block text-[9px] uppercase opacity-70">{isRare ? 'Rare' : 'mm'}</span>
                      </button>
                    );
                  })}
                </div>
              </div>

              {/* Quality Selector */}
              <div>
                <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider mb-2.5">Gemstone Lustre Quality</span>
                <div className="flex gap-2">
                  {qualitiesForActiveStoneSize.map((q) => {
                    const it = catalog.find(i => i.type === 'stone' && i.name === mainStone && i.size === mainSize && i.quality === q);
                    const basePrice = it ? Math.round(it.cost * markup) : 0;
                    return (
                      <button
                        key={q}
                        onClick={() => setMainQuality(q)}
                        className={`flex-1 p-2 text-left border rounded-xl transition-all ${
                          mainQuality === q
                            ? 'bg-obsidian-900 text-gold-100 border-obsidian-900 shadow-sm'
                            : 'border-gold-200/50 bg-white hover:border-gold-400 text-obsidian-700'
                        }`}
                      >
                        <span className="block text-xs font-semibold">{q.replace(' Grade', '')}</span>
                        <span className="block text-[10px] font-mono text-gold-500 mt-0.5">₱{basePrice}/bead</span>
                      </button>
                    );
                  })}
                </div>
              </div>
            </div>
          </div>

              </>
            )}

            {activeTab === 'accents' && (
              <>
                {/* Symmetrical Accents and Spacer Selector */}
          <div className="space-y-4">
            <h3 className="font-mono text-xs text-gold-600 uppercase tracking-widest font-semibold flex items-center gap-2">
              <Sparkles className="w-3.5 h-3.5" /> 3. Architectural Accent Spacers
            </h3>
            
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              {availableSpacers.slice(0, 6).map((sp) => {
                const active = activeSpacerId === sp.id;
                const spPrice = Math.round(sp.cost * markup);
                return (
                  <button
                    key={sp.id}
                    onClick={() => setActiveSpacerId(sp.id)}
                    className={`flex items-center justify-between p-3 border rounded-2xl text-left transition-all duration-300 ${
                      active 
                        ? 'border-gold-500 bg-gold-100/30 shadow-sm' 
                        : 'border-gold-200/40 bg-white hover:border-gold-400'
                    }`}
                  >
                    <div className="flex items-center gap-2.5">
                      <span className="w-4 h-4 rounded-full shadow-inner border border-black/10" style={{ backgroundColor: sp.hex }} />
                      <div>
                        <span className="block text-xs font-medium text-obsidian-900">{sp.name}</span>
                        <span className="block text-[10px] font-mono text-obsidian-400">{sp.size}mm · {sp.material}</span>
                      </div>
                    </div>
                    <span className="font-mono text-xs text-gold-600 font-semibold">₱{spPrice}</span>
                  </button>
                );
              })}
            </div>
          </div>

          <hr className="border-gold-200/30" />

                {/* Luxury Focal Pendant / Charm Selector */}
          <div className="space-y-4">
            <h3 className="font-mono text-xs text-gold-600 uppercase tracking-widest font-semibold flex items-center gap-2">
              <Award className="w-3.5 h-3.5" /> 4. Hand-Finished Center Pendant (Optional)
            </h3>
            <div className="grid grid-cols-1 sm:grid-cols-5 gap-3">
              {/* None Option */}
              <button
                onClick={() => setSelectedCharmId('none')}
                className={`p-3 rounded-2xl border text-center flex flex-col items-center justify-center transition-all ${
                  selectedCharmId === 'none'
                    ? 'border-obsidian-900 bg-obsidian-900 text-gold-100 shadow-sm'
                    : 'border-gold-200/40 bg-white hover:border-gold-300 text-obsidian-700'
                }`}
              >
                <span className="font-mono text-xs block mb-1">∅</span>
                <span className="text-xs font-semibold block">None</span>
                <span className="text-[10px] font-mono opacity-60">No charm</span>
              </button>

              {availableCharms.map((ch) => {
                const active = selectedCharmId === ch.id;
                const chPrice = Math.round(ch.cost * markup);
                return (
                  <button
                    key={ch.id}
                    onClick={() => setSelectedCharmId(ch.id)}
                    className={`p-3 rounded-2xl border text-center flex flex-col items-center justify-center transition-all ${
                      active
                        ? 'border-obsidian-900 bg-obsidian-900 text-gold-100 shadow-sm'
                        : 'border-gold-200/40 bg-white hover:border-gold-300 text-obsidian-700'
                    }`}
                  >
                    <span className="w-3.5 h-3.5 rounded-full mb-1.5 shadow-inner" style={{ backgroundColor: ch.hex }} />
                    <span className="text-xs font-semibold block truncate max-w-full leading-tight">{ch.name.replace(' Pendant', '')}</span>
                    <span className="text-[10px] font-mono text-gold-500 font-semibold block mt-0.5">₱{chPrice}</span>
                  </button>
                );
              })}
            </div>
          </div>

          <hr className="border-gold-200/30" />

                {/* Auto-Style Alignment Center */}
          <div className="bg-gold-100/30 border border-gold-200/40 p-5 rounded-2xl flex flex-col sm:flex-row justify-between items-center gap-4">
            <div>
              <h4 className="font-serif text-base font-semibold text-obsidian-900 flex items-center gap-1.5">
                <Sparkle className="w-4 h-4 text-gold-500 animate-pulse" /> Symmetrical Alignment
              </h4>
              <p className="text-xs text-obsidian-500 mt-1 max-w-md">
                Deploy Fibonacci index spacings and quad-pillar balances to distribute your active spacer.
              </p>
            </div>
            <div className="flex gap-2 w-full sm:w-auto">
              <button
                onClick={applyGoldenRatioAutoStyle}
                className="flex-1 sm:flex-initial bg-obsidian-900 hover:bg-obsidian-800 text-gold-100 text-xs font-semibold py-3 px-5 rounded-xl transition-all shadow-sm"
              >
                Golden Ratio Symmetry
              </button>
              <button
                onClick={autoFitBracelet}
                className="flex-1 sm:flex-initial bg-white border border-gold-300 text-gold-700 hover:bg-gold-50 text-xs font-semibold py-3 px-5 rounded-xl transition-all"
              >
                Standard Auto-Fit
              </button>
            </div>
          </div>

              </>
            )}
            
            {(activeTab === 'stones' || activeTab === 'accents') && (
              <>
                <hr className="border-gold-200/30" />
                {/* Micro sequence editor */}
          <div className="space-y-3">
            <div className="flex justify-between items-center">
              <span className="block text-[10px] font-mono uppercase text-obsidian-400 tracking-wider">Fine-tune Strand Count ({beads.length} Beads)</span>
              <span className="text-xs text-obsidian-400">Tap a bead in the preview disk to edit/swap individual stones.</span>
            </div>
            <div className="flex items-center gap-4 bg-white border border-gold-100 p-4 rounded-2xl shadow-sm">
              <div className="text-sm text-obsidian-700">
                Current total length: <strong className="font-mono font-bold text-obsidian-900">{designStats.length}mm</strong>
              </div>
              <div className="ml-auto flex gap-2">
                <button
                  onClick={handleRemoveBead}
                  className="w-10 h-10 border border-gold-200 rounded-xl bg-white hover:bg-gold-50 flex items-center justify-center text-obsidian-700 font-semibold"
                >
                  <Minus className="w-4 h-4" />
                </button>
                <span className="px-4 py-2 font-mono text-base font-bold bg-gold-50 border border-gold-100 rounded-xl text-obsidian-950">
                  {beads.length}
                </span>
                <button
